# AI Agent Configuration & Logs

## Subagent Configuration
The project sub-agents and skill configurations are defined in [`agents/subagent_config.json`](agents/subagent_config.json).

## Interaction & Prompt Logs
The complete chronological sequence of prompts and AI interactions across the build process is logged in [`PROMPT_LOG.md`](PROMPT_LOG.md).
